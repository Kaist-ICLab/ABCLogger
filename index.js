/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './react-native/App';
import {name as appName} from './react-native/app.json';

AppRegistry.registerComponent(appName, () => App);
